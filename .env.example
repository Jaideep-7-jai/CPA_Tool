# Copy these values into the service's environment; never commit real values.
FLASK_SECRET_KEY=
CPA_DB_HOST=
CPA_DB_USER=
CPA_DB_PASSWORD=
CPA_DB_NAME=CUST_TECH_DB
CPA_MT2_PASSWORD=
CPA_FTP_USERNAME=
CPA_FTP_PASSWORD=
CPA_FTP_HOST=

# Notification routing.  Keep group/member addresses in the service
# environment, not in source control.  CPA users get the FTP-only email view;
# technical/data-team recipients receive local and S3 audit details.
CPA_EMAIL_TECH_RECIPIENTS=
CPA_EMAIL_DATATEAM_RECIPIENTS=
CPA_EMAIL_CPA_RECIPIENTS=
CPA_EMAIL_CPA_USERNAMES=cpauser

# Existing Snowflake/S3 configuration continues to be read from config.py or
# the environment configured for the service.
# This variable is for the zx_dataops_service (HUBUSERS) key. Export it before
# launching Flask so the background worker inherits it. The datateam1 key is
# configured separately in config.SNOWSQL_PASSPHRASE.
SNOWSQL_PRIVATE_KEY_PASSPHRASE=

# Optional ZIP radius processing. The alternate SnowSQL connection defaults
# to 'snowflake'; its service account needs ZIP distance table read permission.
CPA_ZIP_RADIUS_SOURCE_CONNECTION=snowflake
CPA_ZIP_RADIUS_TARGET_CONNECTION=datateam1
# Optional override if the source key differs from the startup environment.
CPA_ZIP_RADIUS_SNOWSQL_PASSPHRASE=
CPA_ZIP_RADIUS_SOURCE_WAREHOUSE=ADHOC_L_WH
CPA_ZIP_RADIUS_SQL_TIMEOUT_SECONDS=300
CPA_ZIP_RADIUS_STAGING_SCHEMA=
CPA_ZIP_RADIUS_S3_BASE=
CPA_ZIP_RADIUS_AWS_KEY_ID=
CPA_ZIP_RADIUS_AWS_SECRET_KEY=
